export const LAST_ACTIVE_THROTTLE_MS = 5 * 60 * 1000;

export const USERS_LOG_CONTEXT = 'Users';

export const USER_PROFILE_API_TAG = {
  name: 'User Profile',
  description: "Endpoints for the signed-in user's own profile.",
} as const;
